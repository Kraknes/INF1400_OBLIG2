        # if abs(self.speed_x) <= self.min_speed:
        #     if self.speed_x >= 0:
        #         self.speed_x = self.min_speed
        #     if self.speed_x <= 0:
        #         self.speed_x = -self.min_speed
                
        # # minimum speed for y   
        # if abs(self.speed_y) <= self.min_speed:
        #     if self.speed_y >= 0:
        #         self.speed_y = self.min_speed
        #     if self.speed_y <= 0:
        #         self.speed_y = -self.min_speed